

This is the core libraries and utilites for using the HAsim modeling
infrastructure. 

For developers: To reconfigure the autoconfigure script use the following:

   # aclocal
   # autoheader
   # automake -a -c --foreign
   # autoconf
   # ./configure
