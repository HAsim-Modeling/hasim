/********************************************************************
 *
 * Awb module specification
 *
 *******************************************************************/


%AWB_START

%name Hasim/FAST combined FPGA Environment
%desc Hasim/FAST combined FPGA Environment
%attributes fast hasim

%provides hasim_fpgaenv

%requires platform_interface virtual_platform fpga_components toplevel_wires fast_utils

%AWB_END
