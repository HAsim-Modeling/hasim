service ISA_EMULATOR
{
    server hw (bsv, connection) <- sw (cpp, method)
    {
        method updateRegister(in UINT64[64] rname_and_rval);
        method emulationFinished(in UINT32[32] unused);
    };
};

